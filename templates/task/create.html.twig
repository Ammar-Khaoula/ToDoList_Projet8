{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'action' : path('task_create')}) }}
            {{ form_widget(form) }}

            <button type="submit" class="col-md-4 btn btn-success">Ajouter</button>
        {{ form_end(form) }}

        <a href="{{ path('task_list') }}" class="col-md-4 btn btn-primary m-3">Retour à la liste des tâches</a>
    </div>
{% endblock %}
